name: ventodemo
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6

  cupertino_icons: ^1.0.8
  vento_annotation:
    path: packages/vento_annotation

dev_dependencies:
  flutter_test:
    sdk: flutter
  vento_generator:
    path: packages/vento_generator
  auto_route_generator:
  build_runner:


  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true
